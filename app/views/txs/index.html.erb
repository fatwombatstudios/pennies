<p style="color: green"><%= notice %></p>

<% content_for :title, "Txs" %>

<h1>Txs</h1>

<div id="txs">
  <% @txs.each do |tx| %>
    <%= render tx %>
    <p>
      <%= link_to "Show this tx", tx %>
    </p>
  <% end %>
</div>

<%= link_to "New tx", new_tx_path %>
