<p style="color: green"><%= notice %></p>

<% content_for :title, "Entries" %>

<h1>Transactions</h1>

<hr>
  <h3>Balances</h3>
  <% @buckets.each do |bucket| %>
    <span><%= bucket.name %></span>
    <span><%= number_to_currency bucket.balance %></span>
    ...
  <% end %>
<hr>

  <%= render "entries", entries: @entries %>

<br>

<%= link_to "Record Income", income_entries_path %>
<%= link_to "Record Expense", expense_entries_path %>
<%= link_to "Transfer Money", transfer_entries_path %>
