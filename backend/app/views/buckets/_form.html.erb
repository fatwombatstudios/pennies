<%= form_with(model: bucket) do |form| %>
  <% if bucket.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(bucket.errors.count, "error") %> prohibited this bucket from being saved:</h2>

      <ul>
        <% bucket.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.label :account_type, style: "display: block" %>
    <%= form.select :account_type, Bucket.account_types.collect { |k,v| [v, k]} %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
